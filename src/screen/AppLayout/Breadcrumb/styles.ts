import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme =>
	// console.log(theme),
	({
		breadcrumb: {
			background: '#a6d4fa'
		},
		font: {
			fontSize: 12
		}
	})
)
